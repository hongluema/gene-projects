export const USE_MOCK = true

export const API_BASE = 'http://localhost:8000'

export const API = {
  wxLogin: `${API_BASE}/api/wx/login`,
  saveUser: `${API_BASE}/users`,
  updateUser: `${API_BASE}/api/users/update`,
}

